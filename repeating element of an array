#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int i, j,size,arr[size];
    cin>>size;
    for(i = 0; i < size; i++){
        cin>>arr[i];
    }
    for(i = 0; i < size; i++) 
        for(j = i + 1; j < size; j++) 
        if(arr[i] == arr[j]) 
            cout << arr[i] << " ";
            return 0;
}
