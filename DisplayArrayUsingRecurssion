#include<iostream>
#include<vector>
using namespace std;
void display_array(int arr[],int index)
{
	if (index == 5)
		return;
	cout << arr[index]<<endl;
	display_array(arr,index+1);
}
int main()
{
	int arr[] = { 1,2,3,4,5 };
	display_array(arr, 0);
	return 0;
}
