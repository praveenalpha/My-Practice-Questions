#include<bits/stdc++.h>
using namespace std;
int main()
{
    int i,j;
     fot(i=0;i<5;i++)
        {for(j=0;j<5;j++)
            {if(i==0||i==4||j==0||j==4)
              {cout<<"1";
                }
              else
               { cout<<" ";
                 }
             }}}
return 0;
}
